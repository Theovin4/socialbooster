"use client";
import { useState } from "react";
export function FundWallet(){const[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function start(form:FormData){setBusy(true);setMessage("");try{const response=await fetch("/api/payments/flutterwave/initialize",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({amount:form.get("amount"),currency:"NGN"})}),data=await response.json();if(!response.ok)throw new Error(data.error||"Payment could not start");window.location.assign(data.url)}catch(error){setMessage(error instanceof Error?error.message:"Payment could not start");setBusy(false)}}return <form action={start} style={{display:"grid",gap:12}}><label>Amount in naira<input className="field" name="amount" type="number" min="100" max="5000000" step="1" placeholder="e.g. 5000" required/></label><div><button className="btn primary" disabled={busy}>{busy?"Opening secure payment…":"Fund with Flutterwave (NGN)"}</button></div>{message?<p style={{color:"#fca5a5"}}>{message}</p>:null}</form>}
