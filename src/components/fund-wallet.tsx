"use client";
import { useState } from "react";
export function FundWallet(){const[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function start(provider:"flutterwave"|"paypal",form:FormData){setBusy(true);setMessage("");try{const response=await fetch(`/api/payments/${provider}/initialize`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({amount:form.get("amount"),currency:form.get("currency")})}),data=await response.json();if(!response.ok)throw new Error(data.error||"Payment could not start");window.location.assign(data.url)}catch(error){setMessage(error instanceof Error?error.message:"Payment could not start");setBusy(false)}}return <form action={(form)=>start(String(form.get("provider")) as "flutterwave"|"paypal",form)} style={{display:"grid",gap:12}}><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}><input className="field" name="amount" type="number" min="1" step="0.01" placeholder="Amount" required/><select className="field" name="currency"><option>USD</option><option>GBP</option><option>EUR</option><option>NGN</option><option>CAD</option><option>AUD</option></select></div><div style={{display:"flex",gap:10}}><button className="btn primary" name="provider" value="flutterwave" disabled={busy}>Fund with Flutterwave</button><button className="btn" name="provider" value="paypal" disabled={busy}>Fund with PayPal</button></div>{message?<p style={{color:"#fca5a5"}}>{message}</p>:null}</form>}
