import{describe,expect,it}from"vitest";import{mapProviderStatus}from"./order-status";describe("provider status mapping",()=>{it.each([["Pending","pending"],["In progress","in_progress"],["Completed","completed"],["Partial","partial"],["Canceled","cancelled"],["Error","failed"]])("maps %s",(input,expected)=>expect(mapProviderStatus(input)).toBe(expected));it("normalizes unknown active states safely",()=>expect(mapProviderStatus("Queued")).toBe("processing"))});
