export async function POST(request:Request){if(!process.env.PAYPAL_WEBHOOK_ID||!process.env.PAYPAL_CLIENT_SECRET)return Response.json({error:"PayPal is not configured"},{status:503});/* Production activation must verify transmission headers with PayPal's verify-webhook-signature endpoint before calling the idempotent wallet RPC. */await request.text();return Response.json({error:"Webhook verification awaiting configured PayPal client"},{status:503})}
