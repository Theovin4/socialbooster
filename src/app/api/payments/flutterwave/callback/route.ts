import { NextResponse } from "next/server"; import { creditVerifiedPayment } from "@/lib/payments/credit"; import { verifyFlutterwaveTransaction } from "@/lib/payments/flutterwave"; import { decimalToMinor } from "@/lib/money";
export async function GET(request:Request){const url=new URL(request.url),id=Number(url.searchParams.get("transaction_id")),reference=url.searchParams.get("tx_ref")||"",app=process.env.NEXT_PUBLIC_APP_URL||url.origin;if(!Number.isInteger(id)||!reference)return NextResponse.redirect(`${app}/dashboard/wallet?payment=cancelled`);try{const verified=await verifyFlutterwaveTransaction(id);if(verified.status!=="successful"||verified.tx_ref!==reference)throw new Error("Unverified payment");await creditVerifiedPayment({provider:"flutterwave",reference,providerTransactionId:String(id),amountMinor:Number(decimalToMinor(String(verified.amount))),currency:verified.currency});return NextResponse.redirect(`${app}/dashboard/wallet?payment=success`)}catch{return NextResponse.redirect(`${app}/dashboard/wallet?payment=pending`)}}
