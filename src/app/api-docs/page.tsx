import type { Metadata } from "next";
import Link from "next/link";
import { SiteHeader } from "@/components/site-header";
import { SiteFooter } from "@/components/site-footer";

const siteUrl = "https://www.socialbooster.net.ng";
export const metadata: Metadata = {
  title: "API Documentation for Social Media Service Resellers",
  description: "Integrate Social Booster API v2 to list social media services, place retry-safe orders, check balances and track delivery from Nigeria or anywhere in Africa.",
  keywords: ["social media services API Nigeria", "SMM API Africa", "social media reseller API", "Instagram services API Nigeria", "Social Booster API documentation"],
  alternates: { canonical: "/api-docs" },
  openGraph: { title: "Social Booster API Documentation", description: "Public API integration instructions for African developers, agencies and social media service resellers.", url: `${siteUrl}/api-docs`, type: "article" },
};

const sections = [
  ["overview", "Overview"], ["authentication", "Authentication"], ["actions", "Available actions"], ["examples", "Examples"], ["responses", "Responses and errors"], ["safety", "Production safety"],
] as const;

export default function ApiDocsPage() {
  const endpoint = `${siteUrl}/api/v2`;
  const structuredData = { "@context": "https://schema.org", "@type": "TechArticle", headline: "Social Booster API v2 documentation", description: "Integration instructions for listing services, checking wallet balance, placing orders and tracking delivery through Social Booster.", url: `${siteUrl}/api-docs`, inLanguage: "en-NG", author: { "@type": "Organization", name: "Social Booster", url: siteUrl }, audience: { "@type": "Audience", audienceType: "Developers, agencies and social media service resellers in Nigeria and Africa" }, about: ["Social media services API", "API integration", "Order automation"] };
  return <><SiteHeader /><main className="shell" style={{ minHeight: "70vh", padding: "90px 0" }}><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(structuredData).replace(/</g, "\\u003c") }} /><span className="eyebrow">Developer platform · API v2</span><h1 style={{ fontSize: "clamp(2.8rem,7vw,5.2rem)", letterSpacing: "-.06em", maxWidth: 980, margin: "18px 0" }}>Social Booster API documentation</h1><p className="muted" style={{ fontSize: 20, lineHeight: 1.7, maxWidth: 820 }}>Connect websites, reseller panels and internal tools to your Social Booster account. List available services, submit orders safely and track fulfilment through one HTTPS endpoint.</p><div style={{ display: "flex", gap: 12, flexWrap: "wrap", margin: "28px 0 48px" }}><Link className="btn primary" href="/register">Create an account</Link><Link className="btn" href="/dashboard/api">Manage API keys</Link></div><div className="api-docs-grid"><article className="glass api-docs-copy"><section id="overview"><h2>Overview</h2><p>API v2 uses HTTPS POST requests to <code>{endpoint}</code>. Requests and responses use JSON. Form-encoded requests are also accepted for compatibility with standard reseller panels. API orders use the authenticated customer wallet and the current customer rate.</p><div className="notice"><strong>Base endpoint</strong><p style={{ marginBottom: 0 }}><code>POST {endpoint}</code></p></div></section><section id="authentication"><h2>Authentication</h2><p>Create an API key from the signed-in <Link href="/dashboard/api">API dashboard</Link>. Send it in the Authorization header. Keys are shown once, must be stored securely and should never be placed in browser-side JavaScript or a public repository.</p><pre className="api-code"><code>{`Authorization: Bearer sb_live_YOUR_API_KEY`}</code></pre><p>Each key is limited to 120 requests per minute. Revoke a key immediately if it is exposed.</p></section><section id="actions"><h2>Available actions</h2><div className="data-table-wrap"><table className="data-table"><thead><tr><th>Action</th><th>Required values</th><th>Returns</th></tr></thead><tbody><tr><td><code>services</code></td><td>None</td><td>Active services, customer rates, limits and capabilities</td></tr><tr><td><code>balance</code></td><td>None</td><td>Available wallet balance and currency</td></tr><tr><td><code>add</code></td><td>service, link, quantity</td><td>Local order reference and submission status</td></tr><tr><td><code>status</code></td><td>order</td><td>Status, charge, start count and remaining quantity</td></tr></tbody></table></div></section><section id="examples"><h2>Examples</h2><h3>List services</h3><pre className="api-code"><code>{`curl -X POST ${endpoint} \\\n  -H "Authorization: Bearer YOUR_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -d '{"action":"services"}'`}</code></pre><h3>Place an order</h3><pre className="api-code"><code>{`curl -X POST ${endpoint} \\\n  -H "Authorization: Bearer YOUR_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -d '{\n    "action":"add",\n    "service":"SERVICE_ID",\n    "link":"https://example.com/your-public-target",\n    "quantity":1000,\n    "idempotency_key":"your-unique-order-001"\n  }'`}</code></pre><h3>Check an order</h3><pre className="api-code"><code>{`{\n  "action": "status",\n  "order": "YOUR_ORDER_REFERENCE"\n}`}</code></pre></section><section id="responses"><h2>Responses and errors</h2><p>Successful requests return JSON with the requested data. Error responses include a stable machine-readable <code>code</code> and a safe message. Common HTTP statuses are <code>400</code> for invalid input, <code>401</code> for an invalid or revoked key, <code>404</code> when an order is not available to that account, <code>429</code> for rate limiting and <code>500</code> for a temporary server problem.</p></section><section id="safety"><h2>Production safety</h2><ul><li>Use a unique <code>idempotency_key</code> of 8–80 letters, numbers, underscores or hyphens for every order.</li><li>Retry temporary failures with exponential backoff; do not create a new idempotency key for the same intended order.</li><li>Validate service minimum and maximum quantities before submitting.</li><li>Keep API keys on your server and use separate keys for separate applications.</li><li>Poll order status responsibly instead of sending continuous requests.</li></ul><div className="notice"><strong>Need integration help?</strong><p className="muted" style={{ marginBottom: 0 }}>Open a private support ticket from your dashboard. Never send an API key through email, WhatsApp or a support message.</p></div></section></article><aside className="glass api-docs-nav"><strong>API v2 guide</strong>{sections.map(([id, label]) => <a key={id} href={`#${id}`}>{label}</a>)}<strong style={{ marginTop: 16 }}>Related</strong><Link href="/services">Browse services</Link><Link href="/dashboard/api">API key management</Link><Link href="/acceptable-use">Acceptable use</Link><Link href="/contact">Contact support</Link></aside></div></main><SiteFooter /></>;
}
