import{AppShell}from"@/components/app-shell";export default async function Page({params}:{params:Promise<{section:string}>}){const{section}=await params;return <AppShell admin><span className="eyebrow">Admin console</span><h1 style={{fontSize:42,textTransform:"capitalize"}}>{section.replaceAll("-"," ")}</h1><div className="glass card"><p className="muted">This area requires a database-assigned administrator role and server-side authorization.</p></div></AppShell>}
