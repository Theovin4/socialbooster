import{AppShell}from"@/components/app-shell";import{adminDb}from"@/lib/firebase/admin";import{approveService,setServiceActive}from"./actions";

export const dynamic="force-dynamic";
export default async function AdminServices(){const db=adminDb();const[providers,approved]=await Promise.all([db.collection("providerServices").orderBy("categoryName").limit(200).get(),db.collection("services").get()]);const approvedMap=new Map(approved.docs.map(doc=>[doc.id,doc.data()]));return <AppShell admin><span className="eyebrow">Catalog control</span><h1 style={{fontSize:42}}>Service approvals</h1><p className="muted" style={{maxWidth:760,lineHeight:1.7}}>Provider synchronization never changes these approval decisions. Review each service carefully before making it visible to customers. Showing the first 200 imported services.</p><div style={{display:"grid",gap:12,marginTop:28}}>{providers.empty?<div className="glass card"><h2>No provider services found</h2><p className="muted">Run the protected service synchronization first, then refresh this page.</p></div>:providers.docs.map(doc=>{const provider=doc.data(),local=approvedMap.get(doc.id),active=local?.active===true;return <article className="glass card" key={doc.id} style={{display:"grid",gridTemplateColumns:"1fr auto",gap:20,alignItems:"center"}}><div><p className="eyebrow" style={{margin:0}}>{provider.categoryName}</p><h2 style={{fontSize:18,margin:"8px 0"}}>{provider.name}</h2><p className="muted" style={{margin:0}}>ID {doc.id} · Provider rate ${provider.rateText}/1,000 · Min {provider.minQuantity} · Max {provider.maxQuantity} · Refill {provider.refillSupported?"Yes":"No"} · Cancel {provider.cancelSupported?"Yes":"No"}</p></div>{local?<form action={setServiceActive}><input type="hidden" name="id" value={doc.id}/><input type="hidden" name="active" value={active?"false":"true"}/><button className={`btn ${active?"":"primary"}`}>{active?"Disable":"Enable"}</button></form>:<form action={approveService}><input type="hidden" name="id" value={doc.id}/><button className="btn primary">Approve at 40% margin</button></form>}</article>})}</div></AppShell>}
