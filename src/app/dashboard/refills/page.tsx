import { AppShell } from "@/components/app-shell"; import { adminDb } from "@/lib/firebase/admin"; import { requireUser } from "@/lib/firebase/session";
export const dynamic="force-dynamic"; export default async function Refills(){const user=await requireUser(),snapshot=await adminDb().collection("refills").where("userId","==",user.uid).limit(100).get();return <AppShell><span className="eyebrow">Aftercare</span><h1 style={{fontSize:42}}>Refills</h1>{snapshot.empty?<div className="glass card"><p className="muted">No refill requests.</p></div>:<div style={{display:"grid",gap:12}}>{snapshot.docs.map(doc=><div className="glass card" key={doc.id}><strong>Order {doc.id.slice(0,8)}</strong><p className="muted">Status: {String(doc.get("status")).replaceAll("_"," ")}</p></div>)}</div>}</AppShell>}
