import { AppShell } from "@/components/app-shell";
import { submitMassOrders } from "../orders/actions";
export default function MassOrderPage() { return <AppShell><span className="eyebrow">Batch workflow</span><h1 style={{ fontSize: 42 }}>Mass order</h1><div className="glass card"><p className="muted">Maximum 10 lines. Format: SERVICE_ID | TARGET_URL | QUANTITY. Each line is validated and submitted independently.</p><form action={submitMassOrders} style={{ display: "grid", gap: 14 }}><textarea className="field" name="orders" rows={10} placeholder="123 | https://example.com/post | 100" required style={{ paddingTop: 14 }} /><label style={{ display: "flex", gap: 10 }}><input type="checkbox" name="confirmed" value="yes" required /> I reviewed every line and confirm submission.</label><button className="btn primary">Submit batch</button></form></div></AppShell>; }
