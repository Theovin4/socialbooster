import{AppShell}from"@/components/app-shell";export default function Dashboard(){return <AppShell><span className="eyebrow">Account overview</span><h1 style={{fontSize:42}}>Good to see you.</h1><div className="grid3">{[["Wallet balance","—"],["Active orders","0"],["Completed orders","0"]].map(x=><div className="glass card" key={x[0]}><p className="muted">{x[0]}</p><strong style={{fontSize:32}}>{x[1]}</strong></div>)}</div><div className="glass card" style={{marginTop:20}}><h2>Firebase connected account</h2><p className="muted">Private wallet and order data load only after Firebase verifies the HTTP-only server session. No sample financial data is shown.</p></div></AppShell>}
