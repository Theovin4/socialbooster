import{AppShell}from"@/components/app-shell";export default async function Order({params}:{params:Promise<{id:string}>}){const{id}=await params;return <AppShell><span className="eyebrow">Order detail</span><h1>Order {id.slice(0,8)}</h1><div className="glass card"><p className="muted">Order details are returned only when the authenticated user owns this record.</p></div></AppShell>}
